A tensors defined without reference to a coordinate system is also known as an invariant with respect to coordinates. However, a tensor (or vector) may depend on observers (called frames). For example, a velocity vector is coordinate-invariant but not frame-invariant. 

